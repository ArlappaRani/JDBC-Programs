package com.mutipleoperations.userinput;
import java.util.*;
import java.sql.ResultSet;
import java.sql.Statement;

public class ProgramJDBC {
	/*public static void create(Connection con)
	{
		try {
		String query="SHOW TABLES";
		Statement stmt=con.createStatemt();
		ResultSet rs=stmt.exeuteQuery();
		while(rs.next())
		{
			System.out.println(query);
		}
		}
		catch(Exception e)
		{
			e.printStackTrace();
		}
		
	}
	
      public static void read(Connection con)
	{
    	  
	showTable(con);
    	  try {
    	Scanner scan=new Scanner(System.in);
		System.out.println("Enter the table name to be displayed");
		 String tableName=scan.next();
		String query="Select* from" +tableName;//place holder
		Statement stmt=con.createQuery(query);
		ResultSet rs=stmt.executeQuery(query);
		while(rs.next())
		{
			System.out.println(rs.getInt(1)+" "+rs.getString(2));
		}
    	  }
    	  catch(Exception e)
    	  {
    		  e.printStackTrace();
    	  }
	}
	//public static void insert(Connection con)
	{
		showTable(con)
		try
		{
			System.out.println("Ente the table name to be updated");
			String tableName=scan.
			
		}
		
		
		
	}
	//public static void update(Connection con)
	{
		
	}
	*/

}
